
class Text_Wrangling:
    def __init__(self,####):
        
#####################################

#Drop tokens if maximum token frequency less than
    def drop_arti_low_freq_tokens(self, n):
                 #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
                 #Output the same array except drop the words with letters less than n
        return Output

#Drop tokens if total frequqncy less than n
    def drop_corp_low_freq_tokens(self, n):
                 #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
                 #Output the same array except drop the words with letters less than n
        return Output

#Drop tokens if maximum token frequency higher than
    def drop_arti_hi_freq_tokens(self, n):
                 #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
                 #Output the same array except drop the words with letters greater than n
        return Output

#Drop tokens if total frequqncy higher than n
    def drop_corp_hi_freq_tokens(self, n):
                 #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
                 #Output the same array except drop the words with letters greter than n
        return Output


#Drop tokens with letters less than n
    def drop_min_word_length(self,n):
                 #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
                 #Output the same array except drop the words with letters greter than n
                 
                 
                 #Drop tokens based on common count
    def drop_tokens_with_issues(self,n):
        #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
        #Output the same array except drop the words with letters less than n
                 
#When ever we have a new idea input should always have the following format
        #Input [[dummy, token 1, token 2, token 3, ], [article name, token 1 freq, token 2 freq, ....],[],.....]
#Output should also be the same, except the tokens removes by the function.
                 
                 